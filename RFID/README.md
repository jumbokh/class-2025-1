# pip install paho-mqtt -i http://mirrors.aliyun.com/pypi/simple/ --trusted-host mirrors.aliyun.com
# pip install paho-mqtt mysql-connector-python -i http://mirrors.aliyun.com/pypi/simple/ --trusted-host mirrors.aliyun.com
```
ALTER USER 'root'@'localhost' IDENTIFIED BY 'fox123';
FLUSH PRIVILEGES;
```
##
```
py -m pip install flask pyscard
python -u exam_review_flask.py --host 0.0.0.0 --port 5000 --quiet
# 瀏覽器打開 http://127.0.0.1:5000/
```
